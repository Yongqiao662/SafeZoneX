# Example env file for SafeZoneX backend
# Copy this to .env and replace placeholder values before running

# MongoDB Configuration
MONGODB_URI=mongodb+srv://YongQiao:YOUR_DB_PASSWORD@safezonex.s70xg0u.mongodb.net/?retryWrites=true&w=majority&appName=SafeZoneX

# Server Configuration
NODE_ENV=development
PORT=8080
HOST=localhost

# API Configuration
API_VERSION=v1
API_BASE_URL=http://localhost:8080/api

# Security Configuration
# Replace these with secure random values in production
JWT_SECRET=replace_with_a_strong_jwt_secret
SESSION_SECRET=replace_with_a_strong_session_secret
ENCRYPTION_KEY=replace_with_a_32_byte_encryption_key
