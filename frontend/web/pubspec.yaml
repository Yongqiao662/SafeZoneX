name: safezonex_web
description: SafeZoneX Web Emergency Monitoring Dashboard

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  web_socket_channel: ^2.4.0
  http: ^1.1.0
  
  # New dependencies for enhanced dashboard
  image_picker: ^1.0.4          # For photo uploads from camera/gallery
  image_picker_web: ^3.1.1      # Web-specific image picker support
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Assets for demonstration images
  assets:
    - assets/images/